using UnityEngine;

namespace ChallengeTwo.VisualLayer.AR
{
    //modes for scanning ,
    //when in manual mode the user Manages the scanning process,
    //when in auto real time meshing it will run auto.
    public enum ScanMode
    {
       manualScaning,
       autoRealTimeMeshing
    }
}
